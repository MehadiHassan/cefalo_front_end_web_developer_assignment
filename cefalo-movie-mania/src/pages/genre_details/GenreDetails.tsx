import React from 'react';
import './_genreDetails.scss';

const GenreDetails: React.FC = () => {
    // const parsed = queryString.parse(location.search);
    // console.log(parsed);
    return (
        <div className="genre-content">
            <h1>Welcoem to Genre</h1>
        </div>
    );
};

export default GenreDetails;
